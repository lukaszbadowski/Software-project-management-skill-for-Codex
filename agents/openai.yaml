interface:
  display_name: "Software Project Management"
  short_description: "Run single-operator LLM projects with operator checkpoints, routed tasks, and traceable acceptance"
  default_prompt: "Use $software-project-management to structure this single-operator LLM-driven software project: run the operator checkpoints, set up the pm/ files, break approved stories into small routed tasks with stable IDs and verification, and close with end-to-end acceptance."
